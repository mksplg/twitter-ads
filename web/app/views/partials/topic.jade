div(ng-controller='TopicCtrl')
	h1 {{topic.text}}
	ul(class="list-group")
		li(class="list-group-item", ng-repeat='user in topic.users')
			a(href="/users/{{user.screen_name}}") {{user.screen_name}}
			span(class="badge")
				{{user.count}}